<div id="titles">
	<h1>Titles</h1>
	<div><%= link_to "New Title", new_title_path %></div>
	<% @titles.each do |title| %>
		<ul class="image_list_item">
			<div id="image_list_container">
				<li>
					<h4><%= title.name %></h4>
						<ul>
							<li class="no-bullet">
							<% title.images.each do |i| %>
								<% i.image_types.each do |t| %>
								<div id="model_images">
									<%= t.name.humanize %><br/>
									<%= link_to image_tag(i.asset_image.url(:thumb)), edit_title_path(title) %>
								</div>
							</li>
							<% end %>
						<% end %>
					</ul>
				</li>
			</div>
		</ul>
	<% end %>
</div>

<div id="celebs">
	<h1>Celebs</h1>
	<div><%= link_to "New Celeb", new_celeb_path %></div>
	<% @celebs.each do |celeb| %>
		<ul class="image_list_item">
			<div id="image_list_container">
				<li>
					<h4><%= celeb.name %></h4>
						<ul>
							<li class="no-bullet">
							<% celeb.images.each do |i| %>
								<% i.image_types.each do |t| %>
								<div id="model_images">
									<%= t.name.humanize %><br/>
									<%= link_to image_tag(i.asset_image.url(:thumb)), edit_celeb_path(celeb) %>
								</div>
							</li>
							<% end %>
						<% end %>
					</ul>
				</li>
			</div>
		</ul>
	<% end %>
</div>