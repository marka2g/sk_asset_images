<p id="notice"><%= notice %></p>

<p>
  <h2>Name:<%= simple_format @title.name %></h2>
  <%#= image_tag(@image.filename.url(:medium)) %>
  <%#= link_to image_tag(@image.filename.url(:medium)), edit_image_path(@image) %>
</p>
<%= link_to 'edit', edit_title_path(@title) %> |
<%= link_to "destroy", @title, method: :delete, confirm: "Are you sure?" %> |
<%= link_to 'all titles', titles_path %> |
<%= link_to 'home', root_path %>

<h2>Title Images</h2>

<%= render "images" %>

