<h1>Images</h1>

<% @images.each do |image| %>
	<div>
		<h3>
			<%= image.name %>
		</h3>
	</div>
  <div class="image">
    <%= image_tag image.asset_image.url(:thumb) %><br/>
  </div>
<% end %>

<p><%= link_to "New Image", new_image_path %></p>
