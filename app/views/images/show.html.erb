<p id="notice"><%= notice %></p>

<p>
  <b>Image:</b><br/>
  <b><%= @image.name %></b>
  <%= link_to image_tag(@image.asset_image.url(:medium)), edit_image_path(@image) %>
</p>

<%= link_to 'edit', edit_image_path(@image) %>
<%= link_to "destroy", @image, method: :delete, confirm: "Are you sure?" %> |
<%= link_to 'all images', images_path %>
