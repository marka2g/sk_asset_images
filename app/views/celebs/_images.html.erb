<% @celeb.images.each do |img| %>
	<ul class="image_list_item">
		<div id="image_list_container">
			<li>
				<h4><%= img.name %></h4>
					<ul>
						<li class="no-bullet">
							<div id="model_images">
							<div class="thumb">
								<h4>Thumbnail</h4>
								<%= link_to image_tag(img.name.url(:thumb)), celeb_path(@celeb) %><br/>
							</div>
							<div class="biopic">
								<h4>Biopic</h4>
								<%= link_to image_tag(img.name.url(:biopic)), celeb_path(@celeb) %><br/>
							</div>
							<div class="headshot">
								<h4>Headshot</h4>
								<%= link_to image_tag(img.name.url(:headshot)), celeb_path(@celeb) %><br/>
							</div>
							<div class="boxcover">
								<h4>Boxcover</h4>
								<%= link_to image_tag(img.name.url(:boxcover)), celeb_path(@celeb) %><br/>
							</div>
							<div class="promo">
								<h4>Promo</h4>
								<%= link_to image_tag(img.name.url(:promo)), celeb_path(@celeb) %><br/>
							</div>
							<%= link_to 'edit', edit_celeb_path(@celeb) %>
						</div>
					</li>
				</ul>
			</li>
		</div>
	</ul>
<% end %>