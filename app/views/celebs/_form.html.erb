<%= form_for @celeb do |f| %>
  <% if @celeb.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@celeb.errors.count, "error") %> prohibited this celeb from being saved:</h2>
      <ul>
      <% @celeb.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label "Name" %>
    <%= f.text_field :name %>
    <br/>
    <%= f.fields_for :images do |builder| %>
      <fieldset>
        <%= builder.label :name, "Image Name" %><br/>
        <%= builder.text_field :name %>
        <%= builder.label :asset_image, "Upload New Title Image" %><br/>
        <%= builder.file_field :asset_image %>
      </fieldset>

    <% end %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


